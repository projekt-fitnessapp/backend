import { describe, test } from 'mocha';
import { expect } from 'chai';
import demo from "../src/mem"
describe('test/demo.test.ts needs to be deleted', () => {
    test('I am a test :)', ()=>{
        demo()
        expect(true).equal(true);
    });
})