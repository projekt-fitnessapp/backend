coverage:
  status:
    project:
      default:
        # basic
        target: 80%
        base: auto 
        paths: 
          - "src"
       # advanced settings
        if_ci_failed: error
        only_pulls: true